#!/usr/bin/python3
def multiply_by_2(a_dictionary):
    new_dict = sorted(a_dictionary)
    for i in new_dict:
        if new_dict[i] is not None
            print("{}: {}".format(i, new_dict[i] * 2))
